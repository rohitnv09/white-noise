{
  "name": "nature-sounds-player",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "shared",
    "client"
  ],
  "scripts": {
    "dev": "npm run dev -w client",
    "build": "npm run build -w shared && npm run build -w client",
    "lint": "eslint . --ext .ts,.tsx"
  },
  "devDependencies": {
    "typescript": "~5.6.0"
  }
}
